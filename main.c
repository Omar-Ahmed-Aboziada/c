#include <stdio.h>
#include <stdlib.h>

struct Employee
{
    int ID;
    char Name[20];
    int Salary;
};

typedef struct Employee Employee;

int main()
{
    /*Employee *emps[100];

    emps[0] = malloc(1 * sizeof(Employee));
    emps[1] = malloc(1 * sizeof(Employee));*/




    return 0;
}
